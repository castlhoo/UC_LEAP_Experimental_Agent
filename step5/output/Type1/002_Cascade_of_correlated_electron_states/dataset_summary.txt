1. Dataset Overview
- Format: Excel spreadsheets containing replot-ready digitized data
- Number of runs / experiments: 4 plotted datasets across 2 files/sheets
  - File 1: 2 datasets (`Fig2b`, `Fig2b inset`)
  - File 2: 2 datasets (`Fig 3e`, `Fig 3f`)
- Approximate data volume:
  - Small tabular dataset, ~500 total rows across all sheets
  - Each sheet is low-dimensional, mostly 2-column or multi-series line data
- Time range:
  - Not explicitly provided
  - Temperatures are present in some sheets, but no acquisition timestamps

2. Instrumentation Setup
- Scanning tunneling microscope / SI-STM
  - Inferred from the paper context and the presence of `dI/dV` spectra
  - Used to measure local electronic density of states via differential conductance
- Spectroscopy / lock-in detection setup
  - Inferred from `dI/dV` measurements
  - Likely used to obtain tunneling conductance as a function of bias
- Fourier / reciprocal-space analysis of STM maps
  - Inferred from `q` and `QBragg`-normalized axes
  - Used to analyze charge order / quasiparticle interference features in momentum space
- Temperature control stage
  - Inferred from temperature-labeled series in `Fig 3e` and `Fig 3f`
  - Used to compare electronic structure at multiple temperatures

3. Information of each run / experiment

a. Measured Parameters
- `Fig2b`
  - Independent variable: `Bias` (mV)
  - Dependent variable: `dI/dV` (a.u.)
- `Fig2b inset`
  - Independent variable: `Bias` (mV)
  - Dependent variable: `dI/dV` (a.u.)
- `Fig 3e`
  - Independent variable: `q` as “fraction of Qbragg”
  - Dependent variable: `Intensity` (a.u.)
  - Multiple series at temperatures: `4.5K`, `12.5K`, `18.5K`, `38.9K`, `48.5K`, `58.7K`
- `Fig 3f`
  - Independent variable: `q` as “fraction of QBragg”
  - Dependent variables: `Intensity` along three directions/labels `qa`, `qb`, `qc` (all a.u.)
  - The sheet structure suggests three separate intensity traces in one table

b. Experimental Context (Inference)
- Likely experiment type:
  - STM spectroscopy and reciprocal-space analysis of STM-derived data
- Possible physical phenomena:
  - Local density of states features in CsV3Sb5
  - Charge order / charge density wave signatures
  - Anisotropic quasiparticle interference
  - Temperature evolution of correlated electronic states
- Reasoning:
  - `dI/dV` vs `Bias` is standard STM spectroscopy
  - `q` normalized by `QBragg` strongly suggests Fourier-space analysis of spatial modulations
  - Multiple temperatures indicate tracking of phase evolution with temperature
  - The paper context explicitly concerns charge order and symmetry breaking in kagome superconductor CsV3Sb5

c. Notes for Analysis
- Structure patterns:
  - Data are already digitized and replot-ready, not raw instrument output
  - Some sheets use repeated column names (`Intensity`) with temperature labels in the first data rows
  - `Fig 3f` mixes multiple `q`/`Intensity` pairs and likely requires careful column parsing
- Potential challenges / ambiguities:
  - No explicit metadata for sample orientation, scan size, or normalization details
  - `QBragg` is referenced but not numerically defined in the files
  - Units are partly implicit (`a.u.`) and may need manual annotation
  - The meaning of `qa`, `qb`, `qc` is not fully specified in the spreadsheet alone
- Suggestions for analysis:
  - Plot `dI/dV` vs `Bias` for both spectra sheets and compare spectral features
  - For `Fig 3e`, compare intensity-vs-`q` curves across temperatures to identify temperature-dependent peak evolution
  - For `Fig 3f`, compare anisotropy among `qa`, `qb`, and `qc`
  - If generating downstream tasks, treat these as figure-digitized line datasets rather than full experimental runs